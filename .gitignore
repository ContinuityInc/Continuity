# Xcode / build
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.moved-aside

# XcodeGen output (the project is regenerated from project.yml)
*.xcodeproj

# Swift Package Manager
.build/
.swiftpm/

# macOS
.DS_Store

# Continuity caches (downloaded audio, stems, analysis) — never commit
/Cache/
*.m4a
*.caf

# TestFlight CI signing material. The .p12 stays in the Actions cache.
.signing/
Signing/ExportOptions.plist

# Python (ASC listing script)
__pycache__/
*.py[cod]
